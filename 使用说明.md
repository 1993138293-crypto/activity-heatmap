# 活动热力图 · 使用说明

## 怎么打开

用鼠标**双击 `index.html`**，它会在浏览器（推荐 Edge 或 Chrome）中打开。
这个文件夹里共有三个文件，各司其职：

- `index.html` —— 页面结构（一般不需要动）
- `app.css` —— 外观样式（颜色、布局都在这里）
- `app.js` —— 功能逻辑（数据、计算、交互）

不需要安装任何软件，关掉浏览器再打开，数据依然还在。

## 首次使用

第一次打开会看到欢迎页，有两个选择：

- **载入示例数据**（推荐先点这个）——自动生成近一年的示例活动（含标签和时长），马上能看到效果，之后可在「全部记录 → 清空数据」一键删除
- **从空白开始**——直接开始自己记录

## 基本操作

| 想做什么 | 怎么做 |
|---|---|
| 记录一项活动 | 点击任意日期格子 →「＋ 添加活动记录」→ 填内容、选强度、可选填时长和标签 → 保存 |
| 修改 / 删除记录 | 点击该日期 → 点击那条记录 → 修改后保存，或点「删除记录」 |
| 按标签筛选 | 图表上方有一排标签胶囊（全部 / 运动 / 学习…），点哪个就看哪个——热力图、统计、记录清单都会同步只显示该标签的内容 |
| 查看历史 | 顶部「◀ ▶」翻月/翻年；月视图下还能左右滑动切换月份 |
| 回到今天 | 点图表右上角的「今天」 |
| 切换月/年视图 | 点「月视图 / 年视图」切换条 |
| 看所有记录清单 | 点右上角列表图标（三条横线），这里也是备份入口 |
| 设置每日提醒 | 点右上角铃铛图标 →「＋ 添加提醒」→ 选时间、写内容、勾选星期几 |
| 换深色/浅色 | 点右上角太阳图标，按一次换一种（自动 → 浅色 → 深色） |
| 备份数据 | 「全部记录」页底部 →「导出备份」，会下载一个文件保存好；换电脑后用「导入备份」恢复 |

## 强度算法（时长加权）

每条记录的得分 = 强度 × 时长系数：

- 不填时长：系数为 1（记录强度 1/2/3/4 级，颜色就是 1/2/3/4 级）
- 填 30 分钟：系数 ×1.25；填 60 分钟以上：系数 ×1.5（封顶）

当天总分 = 所有记录得分相加，按总分映射颜色：0 分无色 → 1.5 分以下最浅 → 2.5 以下次浅 → 3.5 以下次深 → 3.5 以上最深。

举例：跑步（强度 2，60 分钟）= 3 分；加上阅读（强度 1）= 4 分，当天显示为 3 级深绿。同样强度下，时间越长颜色越深，这就是「时长加权」。

## 每日提醒

- 提醒在**应用页面开着的时候**生效：到点弹出顶部横幅（显示今天是否已打卡），桌面浏览器还会弹系统通知
- 横幅 10 秒后自动消失；点击「打开今天」直接进入今天的记录页
- 每条提醒可独立设置时间、内容、重复星期几、开关
- 「发送测试提醒」按钮可以立即预览效果
- **完整推送（页面关着也能收到）待部署后接入**：等应用部署到 HTTPS 服务器并安装到手机主屏后，代码里已预留好推送接口（`app.js` 中的 `PUSH_READY` 开关），接入时只需三步（注册 Service Worker → 请求推送权限 → 配置推送服务）

## 几个规则

- **连续打卡**：从今天往前数连续有记录的天数；今天还没记录时从昨天开始数
- **一周从周一开始**，符合国内习惯
- **未来的日期不能记录**
- **标签**：每条记录最多 5 个标签，每个标签最多 8 个字；标签颜色固定跟随标签本身，不会因筛选而变化

## 数据存在哪里？

数据保存在**本浏览器的本地存储**里（localStorage），不上传任何服务器。
注意：
1. 换浏览器或换电脑，数据不会跟着走（用导出/导入备份迁移）
2. 浏览器清理数据 / 无痕模式下数据会丢失——请定期「导出备份」

## 以后想装到 iPhone 上？

1. **免费方案**：把这三个文件上传到免费的网页托管（如 GitHub Pages），iPhone 用 Safari 打开后选「添加到主屏幕」，就能像 App 一样全屏使用（有图标、无浏览器边框），届时还能启用完整的系统推送
2. **原生 App 方案**：以后有 Mac 或想上架 App Store 时，把界面和逻辑改写成真正的 iOS 应用
